/* =========================================================================
   CHAPTER_EXTRA_MORE — expanded scenario bank that tops every chapter up to
   at least 10 graded scenario questions. Merged in getChapterScenarios()
   alongside CHAPTER_EXTRA. Same shape: keyed by topic id, each value an array
   of { q, options[4], correct:0, why, traps:{B,C,D} }. Option order is
   shuffled at render time, so the correct answer is authored first (index 0).
   ========================================================================= */
export const CHAPTER_EXTRA_MORE = {
/* ---- Domain 2 ---- */
'tool-descriptions':[
  {q:`An agent has two tools, <code>analyze_content</code> and <code>analyze_document</code>, each described only as <em>"Analyzes the input and returns results."</em> The model frequently routes PDF-parsing requests to the wrong one. What is the highest-leverage first fix?`,
   options:[`Expand each description with input formats, example queries, edge cases, and an explicit when-to-use-vs-the-other boundary`,`Add a 12-shot prompt of labeled routing examples before every request`,`Insert a deterministic regex routing layer that picks the tool before the model sees the request`,`Merge the two tools into one and branch internally on a mode parameter`],
   correct:0, why:`Tool descriptions are the primary signal for selection, so clarifying formats, examples, and boundaries directly fixes the misrouting at lowest effort.`,
   traps:{B:`Few-shot routing is higher effort and addresses the symptom without fixing the ambiguous descriptions the model actually reads.`,C:`A deterministic layer adds maintenance and removes the model's flexibility when the cheap fix is better descriptions.`,D:`Merging hides the distinction the user genuinely needs and pushes complexity into an opaque mode parameter.`}},
  {q:`A team writes tool descriptions as terse labels (<code>"Get user"</code>, <code>"Get account"</code>) to save tokens. Selection accuracy is poor. Which statement best explains why?`,
   options:[`The description is the primary mechanism the model uses to choose a tool, so terse labels starve it of the signal needed to disambiguate`,`Token count in descriptions directly lowers accuracy, so shorter is always worse regardless of content`,`The model selects tools by array position, so labels are irrelevant to accuracy`,`Tool names alone determine routing, so descriptions never affect selection`],
   correct:0, why:`Descriptions are the model's primary selection input; stripping them to labels removes the distinguishing context required to route correctly.`,
   traps:{B:`It is not raw length but the absence of distinguishing detail; a short-but-clear description can route well.`,C:`Array position does not drive selection; the model reads descriptions, not order.`,D:`Names contribute but descriptions are the primary mechanism, not an inert field.`}},
  {q:`One tool description reads <em>"Use this for any data-related request."</em> The model overuses it, calling it even for unrelated tasks. What design change reduces the over-triggering?`,
   options:[`Replace the broad catch-all wording with a specific purpose, defined inputs/outputs, and explicit non-use cases`,`Lower the model's temperature so it calls fewer tools overall`,`Move the tool to the end of the tools array so it is considered last`,`Set tool_choice to "any" so the model is forced to pick the best tool`],
   correct:0, why:`Over-triggering comes from over-broad wording; narrowing the description with explicit scope and non-use cases stops the tool from matching unrelated requests.`,
   traps:{B:`Temperature does not address an inherently ambiguous description that matches too many requests.`,C:`Array order does not govern selection, so repositioning changes nothing about over-triggering.`,D:`Forcing "any" makes the model call some tool but does not stop this tool from being the wrong match.`}},
  {q:`A single generic <code>process</code> tool accepts a free-form <code>action</code> string and behaves differently per action; the model often passes invalid action values. Which refactor most improves reliable selection?`,
   options:[`Split it into purpose-specific tools, each with a clear description and defined I/O schema`,`Add more example action strings to the existing tool's description only`,`Keep one tool but rename it to <code>smart_process</code> to signal capability`,`Document the valid actions in the system prompt instead of the tool description`],
   correct:0, why:`Splitting a generic tool into purpose-specific tools with defined I/O gives the model unambiguous targets and valid schemas, the recommended fix for overloaded generic tools.`,
   traps:{B:`More examples help marginally but the free-form action field still invites invalid values and ambiguous routing.`,C:`Renaming changes a keyword association but leaves the overloaded single-tool design intact.`,D:`Moving action docs to the system prompt separates them from the selection signal the model reads at the tool.`}},
  {q:`After adding the phrase <em>"the reporting assistant"</em> to a system prompt, the model began over-selecting a <code>generate_report</code> tool even for non-report queries. What does this illustrate about description and prompt design?`,
   options:[`System-prompt wording can bias tool selection through keyword association, so prompt and tool descriptions must be designed together`,`System prompts have no effect on tool selection; the change was coincidental`,`Tool selection depends solely on the tool name matching the prompt verbatim`,`Adding any system prompt text always increases tool-call frequency uniformly`],
   correct:0, why:`Keyword overlap between the system prompt and a tool can bias selection toward that tool, so the two must be co-designed.`,
   traps:{B:`The observed shift shows the system prompt clearly influences selection, not coincidence.`,C:`Bias arises from keyword association, not a requirement for verbatim name matching.`,D:`The effect is targeted by keyword overlap, not a uniform increase across all tools.`}},
  {q:`A reviewer must choose where to invest to fix tool misrouting and has limited time. Which option is the recommended low-effort, high-leverage first step?`,
   options:[`Improve the tool descriptions with formats, example queries, and boundaries before building heavier infrastructure`,`Build a separate ML classifier that pre-routes every request to a tool`,`Add a human-in-the-loop confirmation step before each tool call`,`Increase the model size to improve implicit tool reasoning`],
   correct:0, why:`Expanding descriptions is the low-effort, high-leverage first step compared with classifiers, routing layers, or larger models.`,
   traps:{B:`A separate classifier is heavyweight infrastructure to try before the cheap description fix.`,C:`Human confirmation slows every call without addressing why the model misroutes.`,D:`A larger model is costly and does not fix descriptions that lack distinguishing detail.`}},
  {q:`A tool <code>search</code> is described accurately but omits that it only accepts ISO date ranges, not natural-language dates. The model passes <em>"last week"</em> and calls fail. What should the description add?`,
   options:[`The expected input format and an example query, plus the edge case that natural-language dates are unsupported`,`A note telling the model to retry with different inputs until one works`,`A higher priority flag so this tool is selected more often`,`Nothing — input validation belongs only in the tool's runtime code`],
   correct:0, why:`Stating input formats, example queries, and edge cases in the description steers the model to supply valid arguments up front.`,
   traps:{B:`Blind retry guidance wastes calls and does not teach the model the required format.`,C:`A priority flag affects selection frequency, not the malformed argument problem.`,D:`Runtime validation catches errors after the fact; the description prevents them by guiding input.`}},
  {q:`Two tools legitimately differ but their descriptions share the same opening sentence and overlapping keywords, causing 50/50 misrouting. Besides expanding detail, what additional change directly reduces the overlap?`,
   options:[`Rename and reword the tools so their names and descriptions no longer share distinguishing keywords`,`Lower the maximum number of tools the agent may call per turn`,`Reorder the two tools so the more common one appears first in the array`,`Combine both descriptions into a single shared paragraph referenced by each`],
   correct:0, why:`Renaming to remove overlapping keywords eliminates the ambiguity that drives near-random routing between similar tools.`,
   traps:{B:`Limiting calls per turn does not help the model tell two similar tools apart.`,C:`Array order does not determine selection, so reordering will not fix the overlap.`,D:`A shared paragraph increases overlap, worsening the very ambiguity to be removed.`}}
],
'structured-errors':[
  {q:`An MCP tool returns <code>{ "isError": true, "message": "Operation failed" }</code> for every failure. The agent cannot decide whether to retry. What metadata should the tool add to enable smart recovery?`,
   options:[`An <code>errorCategory</code> (transient/validation/business/permission), an <code>isRetryable</code> boolean, and a human-readable message`,`A stack trace and the internal exception class name`,`A numeric error code only, leaving interpretation to the agent`,`The full request payload echoed back with no classification`],
   correct:0, why:`Categorizing the error and exposing an explicit retryable flag plus a clear message lets the agent choose the right recovery path.`,
   traps:{B:`A stack trace leaks internals and still leaves the agent guessing whether a retry is safe.`,C:`A bare numeric code without categorization forces the agent to interpret it ad hoc.`,D:`Echoing the payload provides no signal about category or retryability.`}},
  {q:`A payments tool rejects a transfer because it exceeds the customer's daily limit. How should the structured error represent this business-rule violation?`,
   options:[`<code>isRetryable: false</code> with a customer-friendly explanation of the limit`,`<code>isRetryable: true</code> so the agent retries until the limit resets`,`<code>isError: false</code> with an empty result so the flow continues`,`A transient category so the agent backs off and retries shortly`],
   correct:0, why:`A business-rule violation is not retryable and should carry a clear, customer-friendly message rather than inviting retries.`,
   traps:{B:`Marking it retryable causes pointless repeated attempts that will keep failing the same rule.`,C:`Reporting success with an empty result masks the failure and prevents proper handling.`,D:`It is a business rule, not a transient fault, so back-off-and-retry is the wrong category.`}},
  {q:`A query tool returns <code>isError: true</code> when a database lookup finds zero matching rows. Why is this a design flaw?`,
   options:[`A valid query with no matches is a successful empty result, not an error; conflating them blocks correct handling`,`Returning an error for empty results is correct because no data means the call failed`,`Empty results should be returned as <code>isRetryable: true</code> so the agent re-queries`,`Zero rows should always raise a permission-category error`],
   correct:0, why:`An empty result set is a successful query outcome and must be distinguished from an actual access or execution failure.`,
   traps:{B:`No data is a normal valid outcome, not a failed call, so flagging an error is incorrect.`,C:`Re-querying the same successful query will keep returning the same empty set.`,D:`Zero rows is unrelated to permissions; it is simply a successful empty result.`}},
  {q:`A tool sometimes masks access failures by returning an empty success payload (<code>isError: false</code>, <code>results: []</code>). Why does this harm the agent's reliability?`,
   options:[`It makes a real failure indistinguishable from a legitimately empty result, so the agent cannot decide whether to retry`,`It is fine because returning empty data is safer than returning an error`,`It improves reliability by hiding transient noise from the agent`,`It only matters for permission errors, not transient ones`],
   correct:0, why:`Disguising a failure as empty success removes the signal the agent needs to distinguish a retry-worthy failure from a valid no-match.`,
   traps:{B:`Hiding the failure removes the information the agent needs and is not safer.`,C:`Masking failures degrades reliability rather than improving it by suppressing real signals.`,D:`The masking problem applies to any failure type, not just permission errors.`}},
  {q:`An upstream service times out intermittently. How should a well-designed MCP tool classify this error so the agent responds correctly?`,
   options:[`As a transient category with <code>isRetryable: true</code> so the agent can retry with back-off`,`As a validation error so the agent corrects the input`,`As a business error with <code>isRetryable: false</code>`,`As a permission error so the agent requests new credentials`],
   correct:0, why:`An intermittent timeout is a transient fault and should be marked retryable so the agent can safely retry.`,
   traps:{B:`A timeout is not caused by bad input, so a validation category misleads the agent into editing arguments.`,C:`Marking it non-retryable business logic prevents a retry that would likely succeed.`,D:`The failure is a timeout, not an access-rights problem, so credential refresh is irrelevant.`}},
  {q:`A tool returns malformed-argument failures and transient outages under the same generic message. What is the main consequence for the agent?`,
   options:[`Without distinct categories the agent applies one recovery strategy to two different failures and handles at least one wrong`,`The agent will always retry, which is correct for both cases`,`The agent ignores the message entirely and proceeds as if successful`,`Categorization is unnecessary because the model infers the cause from context`],
   correct:0, why:`Different failure types need different responses, so a single generic message forces a one-size strategy that mishandles at least one case.`,
   traps:{B:`Retrying a malformed-argument error repeats the same invalid call and will not succeed.`,C:`A genuine error is not silently ignored; the issue is misclassified recovery, not skipping.`,D:`The model cannot reliably infer category from a generic message; explicit metadata is needed.`}},
  {q:`Which field in a structured MCP error most directly tells the agent whether attempting the call again could succeed?`,
   options:[`The <code>isRetryable</code> boolean`,`The human-readable <code>message</code> string`,`The <code>errorCategory</code> label alone`,`The presence of the <code>isError</code> flag`],
   correct:0, why:`The explicit isRetryable boolean is the field that directly communicates whether a retry is worthwhile.`,
   traps:{B:`A human-readable message aids people but is not a reliable machine signal for the retry decision.`,C:`The category hints at retryability but the boolean states it directly and unambiguously.`,D:`The isError flag only signals that a failure occurred, not whether retrying could help.`}},
  {q:`A permission failure occurs because the caller lacks scope for a resource. How should it be categorized so the agent reacts appropriately rather than retrying blindly?`,
   options:[`As a permission category, typically non-retryable, with a message indicating missing access`,`As a transient category so the agent retries until access is granted`,`As a validation error so the agent edits the resource ID`,`As a successful empty result so the agent moves on`],
   correct:0, why:`A permission failure should be its own non-retryable category so the agent escalates access rather than retrying a call that will keep failing.`,
   traps:{B:`Retrying does not grant missing scope, so a transient classification wastes attempts.`,C:`The arguments may be valid; treating it as validation sends the agent editing the wrong thing.`,D:`Reporting success hides the access failure and prevents the agent from escalating.`}},
  {q:`A reviewer sees a tool that distinguishes <em>access failures</em> from <em>valid empty results</em>. Why is this distinction emphasized in error design?`,
   options:[`Access failures need a retry-or-escalate decision, while valid empty results are successful outcomes needing none; conflating them breaks handling`,`Both should be treated identically since neither returns data`,`Valid empty results should be marked as errors to be safe`,`Access failures should be returned as empty successes to simplify the schema`],
   correct:0, why:`The two require opposite handling, so separating them lets the agent retry or escalate failures while accepting empty results as done.`,
   traps:{B:`Treating them identically destroys the signal that drives correct, opposite responses.`,C:`Marking valid empty results as errors triggers needless retries on successful queries.`,D:`Returning access failures as empty successes masks them, the exact anti-pattern being avoided.`}}
],
'tool-distribution':[
  {q:`A single agent is configured with 18 tools spanning research, writing, billing, and admin roles, and its tool selection has degraded. What is the recommended remedy?`,
   options:[`Scope tools per role with least privilege, splitting work across agents and adding only a few high-frequency cross-role tools`,`Keep all 18 tools but reorder the array so the most common ones come first`,`Set tool_choice to "any" so the model is forced to pick a tool each turn`,`Increase the temperature so the model explores the tool set more thoroughly`],
   correct:0, why:`Too many tools on one agent degrades selection; scoping per role with least privilege and limited cross-role tools restores reliable choices.`,
   traps:{B:`Array order does not determine selection, so reordering 18 tools does not fix degradation.`,C:`Forcing a tool call does not help the model choose correctly among too many options.`,D:`Higher temperature increases randomness, worsening selection rather than improving it.`}},
  {q:`A synthesis agent's only out-of-role need is occasionally confirming a claim. Rather than giving it the full research toolset, what is the appropriate design?`,
   options:[`Add a single focused cross-role tool such as <code>verify_fact</code> for the high-frequency need`,`Grant the synthesis agent every research tool so it never lacks one`,`Remove all tools and have it request data from the user instead`,`Duplicate the entire research agent inside the synthesis agent`],
   correct:0, why:`A few targeted cross-role tools for high-frequency needs preserve least privilege without bloating the toolset.`,
   traps:{B:`Granting the full research toolset reintroduces tool overload and out-of-role misuse.`,C:`Stripping all tools cripples the agent and pushes work onto the user unnecessarily.`,D:`Duplicating a whole agent's tools defeats role scoping and bloats selection.`}},
  {q:`An agent without a role-appropriate need is given a <code>delete_records</code> tool "just in case." What risk does this introduce?`,
   options:[`Agents tend to misuse out-of-role tools, so the unneeded destructive tool invites incorrect or harmful calls`,`None, because extra tools are always ignored when not relevant`,`It only increases latency but never affects behavior`,`It improves safety by giving the agent more recovery options`],
   correct:0, why:`Out-of-role tools tend to be misused, so an unnecessary destructive tool raises the risk of harmful calls and should be removed.`,
   traps:{B:`Extra tools are not reliably ignored; their presence can bias the model into using them.`,C:`The concern is incorrect behavior, not merely latency.`,D:`An out-of-role destructive tool is a hazard, not a safety improvement.`}},
  {q:`A workflow requires the model to call a specific <code>extract_schema</code> tool to produce structured output before proceeding. Which tool_choice setting guarantees that?`,
   options:[`A forced choice: <code>{"type":"tool","name":"extract_schema"}</code>`,`<code>tool_choice: "auto"</code>`,`<code>tool_choice: "any"</code>`,`Listing <code>extract_schema</code> first in the tools array`],
   correct:0, why:`Forcing a specific tool by name guarantees that exact tool is called, ensuring the structured output step happens.`,
   traps:{B:`"auto" lets the model return plain text instead of calling the tool.`,C:`"any" forces some tool call but not necessarily extract_schema.`,D:`Array order does not guarantee call order or that the tool is chosen at all.`}},
  {q:`An engineer assumes that because a tool is listed first in the tools array, it will be called first. Why is this assumption wrong?`,
   options:[`Array order does not guarantee call order; ordering must be enforced via tool_choice or workflow control`,`It is correct — the model always calls tools top to bottom`,`The model calls tools in reverse array order, so it would be called last`,`Call order is fixed by tool name length, not array position`],
   correct:0, why:`Array position carries no ordering guarantee; deterministic sequencing comes from forced tool_choice or explicit workflow steps.`,
   traps:{B:`The model does not iterate tools top to bottom; position is not a call-order contract.`,C:`There is no reverse-order rule either; order is simply not guaranteed by position.`,D:`Name length has no bearing on when or whether a tool is called.`}},
  {q:`A design must let the model answer directly in text when no tool applies, but call a tool whenever one is relevant. Which configuration fits, and what is the key caveat?`,
   options:[`<code>tool_choice: "auto"</code>, with the caveat that it may return text instead of a tool call`,`<code>tool_choice: "any"</code>, which still allows a plain-text answer`,`A forced specific tool, which also permits text responses`,`<code>tool_choice: "auto"</code>, which guarantees a tool is always called`],
   correct:0, why:`"auto" lets the model choose between text and a tool call, with the caveat that it may legitimately respond in text.`,
   traps:{B:`"any" forces some tool call and does not allow the desired plain-text answer.`,C:`A forced specific tool requires that one tool and does not permit a free text answer.`,D:`"auto" does not guarantee a tool call; the whole point is that it may return text.`}}
],
'mcp-config':[
  {q:`A team wants an MCP server available to everyone on the project and tracked in version control. Where should it be configured?`,
   options:[`In <code>.mcp.json</code> at the project root, committed to the repository`,`In <code>~/.claude.json</code> user scope on each developer's machine`,`In a local untracked file that each developer edits independently`,`Hard-coded into the application source so it ships with the build`],
   correct:0, why:`Project scope .mcp.json is committed and shared, making the server available to the whole team through version control.`,
   traps:{B:`User scope ~/.claude.json is personal and not shared via VCS, so the team would not get it.`,C:`A local untracked file defeats sharing and drifts per developer.`,D:`Embedding server config in source is not the MCP configuration mechanism and is unmaintainable.`}},
  {q:`A developer wants to experiment with an MCP server personally without affecting teammates or committing anything. Which scope is appropriate?`,
   options:[`User scope in <code>~/.claude.json</code>, which is personal and not shared via VCS`,`Project scope in <code>.mcp.json</code>, committed to the repo`,`A shared team wiki page describing the server`,`The CI pipeline configuration so it runs everywhere`],
   correct:0, why:`User scope ~/.claude.json keeps the server personal and out of version control, ideal for experimentation.`,
   traps:{B:`Committing to .mcp.json shares it with the whole team, the opposite of personal experimentation.`,C:`A wiki page documents but does not configure or isolate the server.`,D:`CI configuration spreads it to every run rather than keeping it personal.`}},
  {q:`An MCP server needs a GitHub token, but the config file is committed to the repo. How should the token be supplied safely?`,
   options:[`Use environment-variable expansion (e.g. referencing a <code>GITHUB_TOKEN</code> env var) so the secret is not committed`,`Paste the token directly into <code>.mcp.json</code> so it works on every clone`,`Commit the token to a separate file in the same repository`,`Disable the server in version control and re-add the token manually each session`],
   correct:0, why:`Env-var expansion keeps the literal secret out of the committed file while still resolving it at runtime.`,
   traps:{B:`Pasting the token into a committed file leaks the secret to anyone with repo access.`,C:`A separate committed file still places the secret in version control.`,D:`Re-adding the token each session is error-prone and unnecessary when env expansion exists.`}},
  {q:`Two MCP servers are configured — one for Jira, one for an internal service. A developer asks which tools the agent can use. What is true at connection time?`,
   options:[`Tools from all configured servers are discovered at connection and available simultaneously`,`Only the first server's tools load; the second requires an explicit switch command`,`Only one server can be active per session, chosen at startup`,`Tools load lazily and only the most recently used server is reachable`],
   correct:0, why:`At connection time the agent discovers and exposes tools from every configured server simultaneously.`,
   traps:{B:`There is no first-server-only rule; both servers' tools are discovered.`,C:`Multiple servers can be active together; the session is not limited to one.`,D:`Tools are discovered at connection for all servers, not gated to one most-recent server.`}},
  {q:`An agent burns many exploratory tool calls just to list available issues and schemas before doing real work. Which MCP feature is designed to cut this overhead?`,
   options:[`MCP resources, which expose content catalogs like issue summaries and schemas directly`,`Adding more tools so each exploratory step has a dedicated call`,`Raising the per-turn tool-call limit so exploration finishes faster`,`Switching tool_choice to "any" to force quicker tool use`],
   correct:0, why:`MCP resources expose content catalogs such as issue summaries and schemas, reducing the need for exploratory tool calls.`,
   traps:{B:`More tools add selection load and do not replace exploratory discovery with a catalog.`,C:`A higher call limit lets more exploration happen but does not eliminate the need for it.`,D:`Forcing tool use does not provide the catalog content that resources supply.`}},
  {q:`A team needs a standard Jira integration and is deciding between a community MCP server and building a custom one. What is the recommended default?`,
   options:[`Prefer the community server for the standard integration, reserving custom servers for team-specific workflows`,`Always build a custom server to retain full control over every integration`,`Avoid MCP servers entirely and call the Jira REST API by hand each time`,`Build a custom server and contribute it back instead of using the community one`],
   correct:0, why:`Standard integrations like Jira should use community servers; custom servers are reserved for genuinely team-specific workflows.`,
   traps:{B:`Building custom for a standard integration duplicates maintained work unnecessarily.`,C:`Hand-calling the REST API forgoes the MCP tooling the agent is designed to use.`,D:`Reinventing a community-covered integration adds maintenance even if contributed back.`}},
  {q:`A secret referenced via env-var expansion in <code>.mcp.json</code> resolves on the author's machine but fails for a teammate after they clone the repo. What is the most likely cause and fix?`,
   options:[`The teammate hasn't set the referenced environment variable locally; each developer must provide it in their own environment`,`The committed file must contain the literal secret for all clones to work`,`Env-var expansion is unsupported in .mcp.json and must be removed`,`The server must be moved to user scope to read the variable`],
   correct:0, why:`Env-var expansion resolves per environment, so each developer must set the referenced variable locally for it to work after cloning.`,
   traps:{B:`Embedding the literal secret defeats the purpose of expansion and leaks it to the repo.`,C:`Env-var expansion is the supported mechanism; removing it is not the fix.`,D:`Moving scope does not supply a variable the teammate never set; the variable must exist in their environment.`}}
],
/* ---- Domain 3 ---- */
'claudemd':[
  {q:`Your team agreed on a commit-message convention, but a new teammate's Claude Code clone never applies it even though it works perfectly on your machine. Where is the convention most likely defined?`,
   options:[`In your user-level <code>~/.claude/CLAUDE.md</code>, which is personal and not shared via version control`,`In the project-level <code>.claude/CLAUDE.md</code>, which your teammate simply hasn't pulled yet`,`In a directory-level <code>CLAUDE.md</code> that only loads inside subfolders`,`In the project root <code>CLAUDE.md</code>, which is corrupted in the teammate's clone`],
   correct:0, why:`User-level memory lives only on your machine and is never committed, so a teammate's clone won't receive it.`,
   traps:{B:`A project-level file committed to VCS would arrive on clone, so the teammate would have it.`,C:`A directory-level file is still version-controlled and would be present after cloning.`,D:`A committed root file arriving intact is the normal case; corruption is not the likely explanation.`}},
  {q:`A company-wide testing standard must apply to every developer on a repository and survive fresh clones. Where should it be placed?`,
   options:[`Project-level <code>.claude/CLAUDE.md</code> committed to the repository`,`User-level <code>~/.claude/CLAUDE.md</code> on each developer's machine`,`A directory-level <code>CLAUDE.md</code> inside the test folder only`,`An uncommitted local settings file ignored by git`],
   correct:0, why:`Project-level memory is version-controlled and shared with the whole team, making it the right home for team standards.`,
   traps:{B:`User-level files are personal and not shared, so each developer would have to recreate the standard manually.`,C:`A test-folder file only loads when editing files there, missing the repo-wide scope required.`,D:`An ignored local file isn't shared and defeats the goal of a team standard.`}},
  {q:`Your <code>CLAUDE.md</code> has grown to 600 lines and Claude seems to ignore some sections. What is the recommended way to keep it manageable?`,
   options:[`Keep it modular using <code>@import</code> references or split topics into <code>.claude/rules/</code> files`,`Delete the oldest half of the file since memory only reads recent entries`,`Move everything into a single user-level file so it loads faster`,`Convert the file to JSON so Claude parses it more reliably`],
   correct:0, why:`Modularizing with imports or splitting topics into rules files keeps each concern focused and maintainable.`,
   traps:{B:`Memory does not read only recent entries, and deleting content loses real standards.`,C:`Consolidating into a user-level file removes team sharing and doesn't solve the monolith problem.`,D:`CLAUDE.md is Markdown by convention; JSON is not a supported memory format.`}},
  {q:`Claude's behavior is inconsistent across sessions and you suspect a memory file isn't loading. Which step best diagnoses this?`,
   options:[`Run <code>/memory</code> to see which memory files are actually loaded`,`Restart the machine to force all CLAUDE.md files to reload`,`Rename every <code>CLAUDE.md</code> to lowercase so they are detected`,`Delete the project file and rely on the user-level file instead`],
   correct:0, why:`The <code>/memory</code> command lists exactly which memory files are loaded, revealing any that are missing.`,
   traps:{B:`A restart does not surface which files load and is unnecessary for memory resolution.`,C:`Filename casing is not the issue, and renaming risks breaking detection entirely.`,D:`Deleting the project file removes shared standards without diagnosing the load problem.`}},
  {q:`You want a personal scratchpad of shortcuts that should never appear in your teammates' Claude sessions. Where do you put it?`,
   options:[`In your user-level <code>~/.claude/CLAUDE.md</code>`,`In the project root <code>CLAUDE.md</code>`,`In a committed <code>.claude/CLAUDE.md</code>`,`In a <code>.claude/rules/</code> file with a paths glob`],
   correct:0, why:`User-level memory is private to your machine and never shared via version control.`,
   traps:{B:`A root <code>CLAUDE.md</code> is committed and shared with the whole team.`,C:`A committed project file is shared with everyone on the repository.`,D:`Rules files in the repo are version-controlled and visible to teammates.`}},
  {q:`A monorepo has a backend folder needing Python conventions and a frontend folder needing TypeScript conventions, with no overlap. What is the cleanest memory layout?`,
   options:[`A directory-level <code>CLAUDE.md</code> in each subfolder, holding that folder's conventions`,`One giant root <code>CLAUDE.md</code> listing every convention with folder prefixes`,`Both sets of conventions in your user-level file`,`Separate git repositories so each gets its own root file`],
   correct:0, why:`Directory-level memory files scope conventions to the subfolder where work happens, keeping context relevant.`,
   traps:{B:`A single root file loads all conventions everywhere, polluting context with irrelevant rules.`,C:`User-level files aren't shared with the team and mix unrelated conventions personally.`,D:`Splitting the monorepo is a drastic restructure unjustified by a memory-organization need.`}},
  {q:`Which statement correctly describes the precedence and sharing of Claude Code memory files?`,
   options:[`Project-level files are shared via VCS, while user-level files apply only to you and aren't committed`,`User-level files override and replace all project-level files for everyone`,`Directory-level files are the only ones shared with teammates`,`Only the root <code>CLAUDE.md</code> is ever loaded; subfolder files are ignored`],
   correct:0, why:`Project memory is version-controlled and team-shared, whereas user memory is personal and stays out of VCS.`,
   traps:{B:`User-level files affect only your machine and don't apply to anyone else.`,C:`Directory-level files are shared too if committed; they aren't uniquely the shared tier.`,D:`Subfolder <code>CLAUDE.md</code> files do load when you work in those directories.`}},
  {q:`Your root <code>CLAUDE.md</code> mixes logging rules, AWS naming, and cleanup workflows into one long document that's hard to maintain. What refactor best preserves sharing while improving structure?`,
   options:[`Split each topic into its own file and reference them with <code>@import</code> from the project <code>CLAUDE.md</code>`,`Move the whole document to <code>~/.claude/CLAUDE.md</code> to declutter the repo`,`Compress the document into a single paragraph to reduce token usage`,`Duplicate the file into every subfolder so each topic is local`],
   correct:0, why:`Splitting topics into imported files keeps the project memory shared and version-controlled while staying modular.`,
   traps:{B:`Moving it to user-level removes team sharing entirely.`,C:`Compressing into one paragraph harms readability and doesn't address modularity.`,D:`Duplicating across folders creates drift and redundant maintenance without real separation.`}}
],
'commands-skills':[
  {q:`You wrote a <code>/deploy</code> slash command and want every teammate to get it automatically when they clone the repo. Where does its definition belong?`,
   options:[`In <code>.claude/commands/</code>, which is shared via version control`,`In <code>~/.claude/commands/</code>, which is personal to your machine`,`In the root <code>CLAUDE.md</code> as an always-loaded instruction`,`In <code>.claude/skills/</code> with a SKILL.md frontmatter block`],
   correct:0, why:`Project commands in <code>.claude/commands/</code> are committed and distributed to the whole team on clone.`,
   traps:{B:`User-level commands stay on your machine and aren't shared with teammates.`,C:`CLAUDE.md holds standards, not invokable slash commands.`,D:`A skill is an on-demand workflow, not a slash command, and the question asks about a command.`}},
  {q:`A skill runs a long, verbose audit that prints hundreds of lines. You want its output kept out of the main conversation. Which frontmatter setting achieves this?`,
   options:[`<code>context: fork</code> to run the skill in an isolated subagent`,`<code>allowed-tools: none</code> to suppress all output`,`<code>argument-hint: quiet</code> to mute logging`,`<code>context: inline</code> to merge output into the session`],
   correct:0, why:`Setting <code>context: fork</code> runs the skill in an isolated subagent so its verbose output doesn't pollute the main session.`,
   traps:{B:`<code>allowed-tools</code> restricts which tools run; it doesn't suppress or isolate output.`,C:`<code>argument-hint</code> prompts for parameters and has nothing to do with muting logs.`,D:`Merging output inline is the opposite of keeping it out of the main conversation.`}},
  {q:`A skill must never be allowed to run shell commands or delete files during its workflow. Which frontmatter field enforces this?`,
   options:[`<code>allowed-tools</code>, which restricts the tools available during the skill`,`<code>context: fork</code>, which blocks dangerous tools automatically`,`<code>argument-hint</code>, which warns the user before risky steps`,`<code>paths</code>, which limits the skill to safe directories`],
   correct:0, why:`The <code>allowed-tools</code> field whitelists tools, so omitting shell and file-deletion tools prevents them.`,
   traps:{B:`<code>context: fork</code> isolates output but does not by itself restrict which tools are permitted.`,C:`<code>argument-hint</code> only prompts for parameters; it grants no tool restrictions.`,D:`<code>paths</code> is a path-rules frontmatter key, not a skill tool-restriction mechanism.`}},
  {q:`You want a skill to prompt the user for a required <em>ticket ID</em> before it runs. Which frontmatter field is intended for this?`,
   options:[`<code>argument-hint</code>, which signals the required parameter to supply`,`<code>allowed-tools</code>, which lists permitted tools`,`<code>context: fork</code>, which isolates the subagent`,`<code>paths</code>, which scopes the skill by file glob`],
   correct:0, why:`<code>argument-hint</code> tells the user which arguments the skill expects, prompting for required params like a ticket ID.`,
   traps:{B:`<code>allowed-tools</code> governs tool access, not parameter prompting.`,C:`<code>context: fork</code> controls isolation, not user input collection.`,D:`<code>paths</code> belongs to path rules and doesn't prompt for arguments.`}},
  {q:`When should a workflow be a skill rather than an entry in <code>CLAUDE.md</code>?`,
   options:[`When it's an on-demand task workflow invoked only when needed, versus an always-loaded universal standard`,`When it must be loaded on every single request to stay authoritative`,`When it should be hidden from teammates and never shared`,`When it contains no parameters and never produces output`],
   correct:0, why:`Skills are on-demand task workflows invoked when needed, while CLAUDE.md holds always-loaded universal standards.`,
   traps:{B:`Always-loaded content belongs in CLAUDE.md, which is the opposite of a skill's on-demand nature.`,C:`Sharing or hiding is a location concern, not the skill-versus-memory distinction.`,D:`Skills commonly take arguments and produce output; this isn't the deciding factor.`}},
  {q:`A personal experimental command helps only your workflow and you don't want it cluttering the shared repo. Where should it live?`,
   options:[`In <code>~/.claude/commands/</code>, which is personal and not committed`,`In <code>.claude/commands/</code>, so it's version-controlled`,`In <code>.claude/skills/</code> as a forked skill`,`In the project root <code>CLAUDE.md</code> as a standard`],
   correct:0, why:`User commands in <code>~/.claude/commands/</code> stay on your machine and aren't shared via the repository.`,
   traps:{B:`Project commands are committed and shared with the whole team, the opposite of what's wanted.`,C:`A skill is a different construct and placing it in the repo skills folder still shares it.`,D:`CLAUDE.md is for standards, not personal invokable commands, and a root file is shared.`}},
  {q:`A teammate complains your new skill floods their session with diagnostic noise and occasionally tries to run unexpected tools. Which two frontmatter settings together address both problems?`,
   options:[`<code>context: fork</code> to isolate output and <code>allowed-tools</code> to restrict permitted tools`,`<code>argument-hint</code> to mute noise and <code>paths</code> to block tools`,`<code>context: inline</code> to isolate output and <code>allowed-tools</code> to add tools`,`<code>paths</code> to fork output and <code>argument-hint</code> to restrict tools`],
   correct:0, why:`<code>context: fork</code> keeps verbose output isolated while <code>allowed-tools</code> limits which tools the skill may use.`,
   traps:{B:`<code>argument-hint</code> doesn't mute output and <code>paths</code> doesn't block tools.`,C:`<code>context: inline</code> merges output into the session rather than isolating it.`,D:`<code>paths</code> doesn't fork output and <code>argument-hint</code> doesn't restrict tools.`}}
],
'path-rules':[
  {q:`Your test files use a strict convention, but they're scattered across dozens of directories throughout the tree. Which mechanism applies the convention only when editing a test file, without putting a <code>CLAUDE.md</code> in every folder?`,
   options:[`A <code>.claude/rules/</code> file with frontmatter <code>paths: ["**/*.test.tsx"]</code>`,`A single root <code>CLAUDE.md</code> listing the test convention`,`A directory-level <code>CLAUDE.md</code> in the top-level tests folder`,`A user-level <code>~/.claude/CLAUDE.md</code> entry`],
   correct:0, why:`A path rule loads only when you edit a file matching its glob, covering scattered test files without per-folder memory.`,
   traps:{B:`A root <code>CLAUDE.md</code> loads on every request regardless of file type, wasting context.`,C:`A directory-bound file can't easily reach test files spread across many unrelated directories.`,D:`A user-level file isn't shared with the team and still loads unconditionally.`}},
  {q:`What is the key efficiency advantage of <code>.claude/rules/</code> path rules over an always-loaded root <code>CLAUDE.md</code>?`,
   options:[`They load conditionally only when you edit a matching file, cutting irrelevant context and tokens`,`They load on every request but compress better, saving tokens`,`They override user-level memory to reduce conflicts`,`They run in a forked subagent to isolate output`],
   correct:0, why:`Path rules are conditional: they load only on matching edits, so unrelated context and tokens are avoided.`,
   traps:{B:`Path rules are not loaded on every request; conditional loading is the whole point.`,C:`Overriding user memory is unrelated to the token-efficiency benefit.`,D:`Forking subagents is a skills feature, not how path rules work.`}},
  {q:`Which YAML frontmatter key in a <code>.claude/rules/</code> file controls when the rule activates?`,
   options:[`<code>paths</code>, a glob that matches files being edited`,`<code>context</code>, set to fork or inline`,`<code>allowed-tools</code>, a whitelist of tools`,`<code>argument-hint</code>, a parameter prompt`],
   correct:0, why:`The <code>paths</code> glob determines which edited files trigger the rule to load.`,
   traps:{B:`<code>context</code> is a skills frontmatter key for isolation, not rule activation.`,C:`<code>allowed-tools</code> restricts tools in skills; it doesn't gate rule loading.`,D:`<code>argument-hint</code> prompts for skill parameters and isn't a path-rule trigger.`}},
  {q:`A convention applies to every <code>.test.tsx</code> file, but they live under <code>src/</code>, <code>packages/</code>, and <code>apps/</code>. Why does a directory-level <code>CLAUDE.md</code> fall short here?`,
   options:[`A directory-bound file can't easily cover files scattered across many unrelated directories`,`Directory-level files never load when editing test files`,`Directory-level files are not shareable via version control`,`Directory-level files load on every request and are too slow`],
   correct:0, why:`A directory-bound memory file is tied to one folder subtree and can't span conventions scattered across the whole tree.`,
   traps:{B:`Directory files do load when you work within their folder; the issue is coverage breadth, not loading.`,C:`Directory-level files are committed and shareable, so sharing isn't the problem.`,D:`Directory files load when working in their subtree, not on every request; speed isn't the issue.`}},
  {q:`When does a path rule with <code>paths: ["**/*.sql"]</code> actually load into context?`,
   options:[`Only when you edit a file matching the <code>.sql</code> glob`,`On every request, regardless of which file you touch`,`Only at session startup, then it stays resident`,`Only when explicitly invoked as a slash command`],
   correct:0, why:`Path rules load conditionally when an edited file matches the configured glob, not before.`,
   traps:{B:`Loading on every request is exactly what path rules avoid.`,C:`They don't load eagerly at startup; loading is triggered by matching edits.`,D:`Path rules are not slash commands and aren't invoked manually.`}},
  {q:`Your monorepo enforces a migration-file standard for files named <code>*.migration.ts</code> in dozens of service folders. Which is the most token-efficient way to apply it?`,
   options:[`A path rule with <code>paths: ["**/*.migration.ts"]</code> in <code>.claude/rules/</code>`,`A line in the root <code>CLAUDE.md</code> describing the migration standard`,`A directory-level <code>CLAUDE.md</code> copied into every service folder`,`A user-level memory entry on each developer's machine`],
   correct:0, why:`A single path rule applies the standard only when editing migration files anywhere in the tree, minimizing context.`,
   traps:{B:`A root entry loads on every request even when no migration file is involved, wasting tokens.`,C:`Copying a file into every folder causes drift and is far harder to maintain.`,D:`User-level entries aren't shared and still load unconditionally.`}},
  {q:`You add a path rule but it never seems to take effect. Which is the most likely cause given how path rules work?`,
   options:[`The <code>paths</code> glob doesn't match the files you're editing, so the rule never triggers`,`Path rules require a restart before any glob is recognized`,`Path rules only work inside a forked skill subagent`,`Path rules must be invoked as a slash command to load`],
   correct:0, why:`Path rules load only on matching edits, so a glob that doesn't match the edited files means the rule stays dormant.`,
   traps:{B:`A restart is not a documented requirement for glob recognition.`,C:`Path rules are independent of skills and don't require a forked subagent.`,D:`Path rules are conditional on edits, not manual slash-command invocation.`}},
  {q:`Compared with a directory-level <code>CLAUDE.md</code>, when is a <code>.claude/rules/</code> path rule the better choice?`,
   options:[`When a convention spans many directories, like test files scattered throughout the tree`,`When a convention applies to one self-contained subfolder only`,`When you want the convention loaded on every request unconditionally`,`When the rule should be personal and never committed`],
   correct:0, why:`Path rules excel when a convention crosses many directories that a single directory-bound file can't cover.`,
   traps:{B:`A self-contained subfolder is exactly where a directory-level <code>CLAUDE.md</code> works well.`,C:`Unconditional loading is what root memory does, not the conditional path-rule model.`,D:`Path rules placed in the repo are committed; personal-only content belongs in user-level memory.`}},
  {q:`Which statement about how <code>.claude/rules/</code> path rules manage context is correct?`,
   options:[`They load conditionally on matching edits rather than on every request, reducing irrelevant tokens`,`They permanently replace the root <code>CLAUDE.md</code> once added`,`They always load alongside every other memory file at startup`,`They only affect read operations, never edits`],
   correct:0, why:`Path rules are conditional, loading only when an edited file matches the glob, which trims irrelevant context.`,
   traps:{B:`Path rules complement, not replace, the root <code>CLAUDE.md</code>.`,C:`They do not load unconditionally at startup; that defeats their purpose.`,D:`Their trigger is editing matching files, so edits are precisely when they apply.`}}
],
'plan-mode':[
  {q:`You're asked to migrate a 45-file library across the codebase with several viable approaches. Which mode fits best?`,
   options:[`Plan mode, to explore and design before changing anything`,`Direct execution, to start editing files immediately`,`Direct execution, since the change touches many files quickly`,`Neither; large migrations should be done manually outside Claude`],
   correct:0, why:`Large, multi-file, multi-approach tasks call for plan mode to explore and design before making changes.`,
   traps:{B:`Jumping straight to edits skips the design exploration a large migration needs.`,C:`Touching many files is a reason for planning, not for rushing into direct execution.`,D:`Claude can absolutely handle the migration; opting out entirely isn't warranted.`}},
  {q:`A single-file bug with a clear stack trace pointing at the offending line needs fixing. Which approach is appropriate?`,
   options:[`Direct execution, since the change is simple and well-scoped`,`Plan mode, to design alternatives before touching the file`,`Plan mode, because every bug fix requires a design phase`,`Plan mode, then abandon the plan and edit anyway`],
   correct:0, why:`A simple, well-scoped fix with a clear cause is ideal for direct execution without a planning overhead.`,
   traps:{B:`Designing alternatives for a one-line, clearly-diagnosed fix adds needless overhead.`,C:`Not every bug fix needs a design phase; small scoped fixes don't.`,D:`Entering plan mode only to discard it wastes effort for a trivial change.`}},
  {q:`What is the primary purpose of plan mode?`,
   options:[`To explore and design before changing code on large, architectural, multi-approach tasks`,`To execute approved edits faster by skipping confirmation prompts`,`To restrict which tools Claude may use during a task`,`To isolate verbose command output from the main session`],
   correct:0, why:`Plan mode is for exploring and designing ahead of changes on large or architectural work with multiple valid paths.`,
   traps:{B:`Plan mode is about design, not accelerating execution or skipping confirmations.`,C:`Tool restriction is governed by skill <code>allowed-tools</code>, not plan mode.`,D:`Isolating output is the job of a forked subagent, not plan mode itself.`}},
  {q:`During plan mode, what role does the Explore subagent play?`,
   options:[`It isolates verbose discovery work and returns concise summaries to preserve main-conversation context`,`It applies the approved edits automatically once planning ends`,`It enforces the <code>paths</code> glob on every file edited`,`It commits the plan to version control for the team`],
   correct:0, why:`The Explore subagent isolates noisy investigation and feeds back summaries, keeping the main context clean.`,
   traps:{B:`Explore gathers information; it doesn't apply edits.`,C:`Glob enforcement is a path-rules feature, unrelated to the Explore subagent.`,D:`Explore doesn't commit anything to version control.`}},
  {q:`You need to redesign a monolith into microservices, where several architectures are plausible. Why is plan mode preferred over direct execution?`,
   options:[`The task is large and architectural with multiple valid approaches that benefit from upfront design`,`Direct execution can't edit more than one file at a time`,`Plan mode automatically picks the single correct architecture for you`,`Direct execution is disabled for any task touching services`],
   correct:0, why:`Architectural tasks with several valid approaches benefit from exploring and designing before committing to changes.`,
   traps:{B:`Direct execution can edit many files; the limitation isn't file count.`,C:`Plan mode helps you reason about options; it doesn't auto-select the design.`,D:`Direct execution isn't categorically disabled for service work.`}},
  {q:`Can plan mode and direct execution be combined in a single workflow?`,
   options:[`Yes — plan to investigate and agree on an approach, then direct-execute the agreed plan`,`No — choosing plan mode permanently locks the session out of direct edits`,`No — they require separate repositories to coexist`,`Yes, but only if you disable the Explore subagent first`],
   correct:0, why:`You can use plan mode to investigate and settle on an approach, then switch to direct execution to implement it.`,
   traps:{B:`Plan mode doesn't permanently lock out direct edits; you can transition to execution.`,C:`No separate repositories are needed to use both modes.`,D:`Combining the modes doesn't require disabling Explore.`}},
  {q:`Which scenario is the clearest signal to choose direct execution over plan mode?`,
   options:[`A well-scoped single-file change with a clear, unambiguous cause`,`A cross-cutting refactor spanning dozens of modules with trade-offs`,`A monolith-to-microservices redesign with competing architectures`,`A library migration touching 45 files across the tree`],
   correct:0, why:`A small, clearly-diagnosed single-file change is exactly the case where direct execution is most efficient.`,
   traps:{B:`A cross-cutting refactor with trade-offs is precisely when planning pays off.`,C:`Competing architectures demand the design exploration plan mode provides.`,D:`A sprawling 45-file migration is a planning candidate, not a direct-execution one.`}},
  {q:`How does the Explore subagent help preserve the main conversation's context during a large planning effort?`,
   options:[`It performs verbose discovery in isolation and returns only summaries to the main thread`,`It deletes prior messages to free up the context window`,`It compresses the entire conversation into shorthand`,`It moves the plan into a separate user-level memory file`],
   correct:0, why:`By isolating noisy discovery and returning summaries, the Explore subagent keeps the main context uncluttered.`,
   traps:{B:`Explore doesn't delete prior messages; it offloads verbose work to a subagent.`,C:`Compressing into shorthand is a different mechanism, not Explore's function.`,D:`Explore doesn't relocate plans into memory files.`}}
],
'cicd':[
  {q:`You're running Claude Code inside a CI pipeline and the job hangs waiting for input. Which flag makes it run non-interactively?`,
   options:[`<code>-p</code> / <code>--print</code>, which runs once and prints without waiting for input`,`<code>--batch</code>, which disables all prompts`,`<code>CLAUDE_HEADLESS=1</code>, the headless environment flag`,`<code>--interactive=false</code>, which turns off the REPL`],
   correct:0, why:`The <code>-p</code>/<code>--print</code> flag runs Claude non-interactively and prints output, avoiding input hangs in CI.`,
   traps:{B:`<code>--batch</code> is not a real Claude Code flag.`,C:`<code>CLAUDE_HEADLESS</code> is not a real environment variable.`,D:`<code>--interactive=false</code> is not the documented non-interactive flag.`}},
  {q:`Your CI job needs to post Claude's review findings as inline PR comments, so the output must be machine-parseable. Which combination produces that?`,
   options:[`<code>--output-format json</code> with <code>--json-schema</code> to produce parseable findings`,`<code>-p</code> alone, which always emits JSON by default`,`<code>--batch</code> with <code>CLAUDE_HEADLESS</code> for structured output`,`<code>--print</code> with <code>--interactive</code> for formatted comments`],
   correct:0, why:`Combining <code>--output-format json</code> with <code>--json-schema</code> yields structured, parseable findings for inline comments.`,
   traps:{B:`<code>-p</code> prints output but doesn't by itself guarantee a parseable JSON schema.`,C:`Neither <code>--batch</code> nor <code>CLAUDE_HEADLESS</code> exists.`,D:`<code>--interactive</code> contradicts non-interactive CI and doesn't structure output.`}},
  {q:`Why does an independent Claude review instance in CI catch more issues than having the code generator review its own work?`,
   options:[`Session isolation means the reviewer lacks the generator's reasoning context and isn't anchored to its assumptions`,`The reviewer runs on a larger model automatically`,`Self-review is blocked by the API and always errors`,`The isolated instance has access to more files than the generator`],
   correct:0, why:`An isolated review instance doesn't share the generator's reasoning, so it isn't biased by the same assumptions and catches more.`,
   traps:{B:`Isolation doesn't imply a larger model; the benefit is independence, not model size.`,C:`Self-review isn't API-blocked; it's just less effective, not impossible.`,D:`File access isn't the differentiator; freedom from shared reasoning is.`}},
  {q:`On a re-run of an automated PR review, how do you prevent Claude from posting duplicate comments for issues it already flagged?`,
   options:[`Include the prior findings so it reports only new or still-unaddressed issues`,`Add <code>--batch</code> so each run starts completely fresh`,`Set <code>CLAUDE_HEADLESS</code> to deduplicate comments automatically`,`Disable JSON output so comments can't repeat`],
   correct:0, why:`Feeding prior findings into the re-run lets Claude skip resolved items and report only new or unaddressed ones.`,
   traps:{B:`<code>--batch</code> isn't a real flag, and a fresh start would re-flag everything.`,C:`<code>CLAUDE_HEADLESS</code> isn't real and doesn't deduplicate.`,D:`Turning off JSON output breaks parsing without preventing duplicates.`}},
  {q:`What role does <code>CLAUDE.md</code> play when Claude runs an automated review in CI?`,
   options:[`It supplies CI context such as testing standards, fixtures, and review criteria`,`It selects the non-interactive flag for the runner`,`It defines the JSON schema for parseable output`,`It isolates the review session from the generator`],
   correct:0, why:`<code>CLAUDE.md</code> provides the project context — testing standards, fixtures, and criteria — that grounds the CI review.`,
   traps:{B:`Flag selection is done on the command line, not in CLAUDE.md.`,C:`The JSON schema is specified via <code>--json-schema</code>, not CLAUDE.md.`,D:`Session isolation comes from running a separate instance, not from CLAUDE.md.`}},
  {q:`A teammate proposes <code>CLAUDE_HEADLESS=1</code> and <code>--batch</code> to run Claude in CI. What's the problem with this plan?`,
   options:[`Neither is a real flag; the correct non-interactive approach is <code>-p</code>/<code>--print</code>`,`Both work but only on self-hosted runners`,`They work but require disabling JSON output`,`They are valid but slower than interactive mode`],
   correct:0, why:`<code>CLAUDE_HEADLESS</code> and <code>--batch</code> don't exist; <code>-p</code>/<code>--print</code> is the real non-interactive mechanism.`,
   traps:{B:`They don't work on any runner because they aren't real flags.`,C:`Since they aren't real, JSON output considerations are moot.`,D:`Non-existent flags can't be valid or compared on speed.`}},
  {q:`Your CI review step must emit findings that a script can read and map to specific lines for inline comments. Which output configuration is correct?`,
   options:[`<code>--output-format json</code> combined with <code>--json-schema</code>`,`<code>--print</code> with default plain-text output`,`<code>--batch</code> with verbose logging`,`<code>CLAUDE_HEADLESS</code> with markdown output`],
   correct:0, why:`<code>--output-format json</code> plus <code>--json-schema</code> produces structured findings a script can parse and map to lines.`,
   traps:{B:`Plain-text output isn't reliably machine-parseable for line mapping.`,C:`<code>--batch</code> isn't a real flag and verbose logs aren't structured findings.`,D:`<code>CLAUDE_HEADLESS</code> doesn't exist and markdown isn't a parseable schema.`}},
  {q:`Which practice best improves the quality of an automated Claude review in CI?`,
   options:[`Run it as an independent, session-isolated instance separate from whatever generated the code`,`Always reuse the generator's exact session so it remembers its choices`,`Skip <code>CLAUDE.md</code> so the review stays unbiased`,`Use interactive mode so a human can intervene mid-run`],
   correct:0, why:`A session-isolated reviewer without the generator's reasoning context catches more issues than self-review.`,
   traps:{B:`Reusing the generator's session re-introduces the same blind spots that hurt self-review.`,C:`Dropping <code>CLAUDE.md</code> removes the testing standards and criteria the review needs.`,D:`Interactive mode hangs CI and defeats the point of an automated pipeline step.`}}
],
/* ---- Domain 4 ---- */
'explicit-criteria':[
  {q:`Your AI code reviewer flags too many trivial nitpicks. A teammate suggests adding <code>Be conservative and only report high-confidence findings</code> to the system prompt. Why is this likely to underperform?`,
   options:[`Vague instructions like "be conservative" do not reliably improve precision; you must define exactly which categories to report versus skip`,`Conservative phrasing always increases temperature, raising randomness`,`It will eliminate true positives along with false positives at an equal rate`,`Confidence language only works when paired with a JSON schema`],
   correct:0, why:`Explicit categorical criteria (report bugs/security, skip minor style) outperform vague conservatism, which does not actually move precision.`,
   traps:{B:`Prompt wording does not change the temperature parameter.`,C:`The problem is unspecified criteria, not a guaranteed equal-rate loss of true positives.`,D:`A schema governs output shape, not which findings qualify as worth reporting.`}},
  {q:`A reviewer prompt produces accurate security findings but also many false-positive "style" complaints, and developers are starting to ignore <em>all</em> of its output. What is the best first move?`,
   options:[`Define explicitly which issues to report (bugs, security) and which to skip (minor style), with concrete examples per severity`,`Lower the temperature to 0 so the model stops inventing style issues`,`Run the prompt three times and majority-vote each finding`,`Add "do not be pedantic" to the end of the prompt`],
   correct:0, why:`Explicit inclusion/exclusion criteria with examples directly fix which categories get reported, restoring trust.`,
   traps:{B:`Temperature controls randomness, not whether style nitpicks meet the bar for reporting.`,C:`Voting on an ambiguous criterion just averages the same ambiguity; it does not define what counts.`,D:`"Do not be pedantic" is exactly the vague instruction that fails to improve precision.`}},
  {q:`Across categories, your reviewer's "security" findings are excellent but its "naming-convention" findings are wrong half the time, eroding trust in the accurate categories. Pending a prompt fix, what is a sound interim step?`,
   options:[`Temporarily disable the noisy naming-convention category while you fix its prompt`,`Increase the weight of security findings so they dominate the output`,`Raise temperature for naming checks to add variety`,`Merge all categories into a single combined severity score`],
   correct:0, why:`A noisy category can be temporarily disabled so its false positives stop eroding trust in accurate categories while you repair its prompt.`,
   traps:{B:`Weighting does not stop the bad naming findings from appearing and undermining trust.`,C:`More randomness makes the noisy category worse, not better.`,D:`Collapsing categories hides which checks are reliable and which are not.`}},
  {q:`Two candidate instructions for a bug-detection prompt: (A) <code>Report null-dereference, resource-leak, and injection bugs; skip formatting and naming</code>, or (B) <code>Report important issues only</code>. Which is more likely to yield consistent precision, and why?`,
   options:[`A, because explicit categorical criteria define exactly what qualifies, unlike the subjective word "important"`,`B, because shorter prompts always generalize better`,`B, because "important" lets the model use broader judgment`,`Neither differs; both depend only on temperature`],
   correct:0, why:`Naming concrete categories to include and exclude is what produces consistent precision; "important" is undefined.`,
   traps:{B:`Brevity is irrelevant; an undefined term is the problem.`,C:`Broad subjective judgment is exactly what causes inconsistency here.`,D:`Criteria clarity, not temperature, drives this difference.`}},
  {q:`A stakeholder insists that adding <code>only report high-confidence findings</code> will cut your reviewer's false positives. Before changing the categorical criteria, what should you tell them?`,
   options:[`That phrase does not reliably improve precision on its own; the fix is defining concrete report/skip categories with examples`,`That phrase guarantees a measurable precision gain of about 30%`,`Confidence wording only works at temperature 0`,`High-confidence wording must be paired with majority voting to take effect`],
   correct:0, why:`"Only report high-confidence findings" is the canonical vague instruction that does not actually improve precision.`,
   traps:{B:`There is no guaranteed precision gain from that phrasing.`,C:`Temperature does not activate confidence wording.`,D:`Voting does not rescue an ambiguous criterion.`}},
  {q:`You want each reported issue to carry a meaningful severity so triage is reliable. What is the most effective prompt design?`,
   options:[`Define each severity level (e.g., critical, major, minor) with concrete examples of what belongs in each`,`Instruct the model to "use good judgment on severity"`,`Ask for a numeric 1-100 score with no rubric`,`Let severity emerge from running the prompt at higher temperature`],
   correct:0, why:`Concrete per-severity examples turn an ambiguous label into a consistent, applicable rule.`,
   traps:{B:`"Good judgment" is the vague instruction that fails to standardize severity.`,C:`An unanchored numeric score is just as subjective as a vague label.`,D:`Temperature adds variance, not a usable severity rubric.`}},
  {q:`A team reports that the reviewer keeps flagging intentional, idiomatic patterns as bugs. Which criteria change best reduces this?`,
   options:[`Explicitly list which patterns are acceptable and excluded from reporting, with examples`,`Tell the model to "avoid false positives"`,`Switch the model to a smaller, faster variant`,`Increase the number of findings requested per file`],
   correct:0, why:`Naming the acceptable patterns to exclude gives the model a concrete boundary, cutting those false positives.`,
   traps:{B:`"Avoid false positives" is vague and does not specify which patterns are acceptable.`,C:`Model size does not encode which patterns are intentional.`,D:`Requesting more findings increases, not decreases, false positives.`}},
  {q:`Why does a single noisy reporting category pose an outsized risk to an AI code reviewer's adoption?`,
   options:[`Its false positives erode developer trust in the reviewer's other, accurate categories too`,`It increases the token cost of every other category`,`It forces the model into a higher temperature globally`,`It permanently corrupts the model's weights for that repo`],
   correct:0, why:`High false-positive categories erode trust in the accurate ones, so one bad category can sink overall adoption.`,
   traps:{B:`A noisy category does not raise other categories' token cost.`,C:`Categories do not change the temperature parameter.`,D:`Prompting does not alter model weights.`}},
  {q:`You are drafting criteria for a new "dependency-risk" check. Which approach most directly produces dependable output?`,
   options:[`State the exact conditions to report (e.g., known-CVE versions, unmaintained packages) and what to skip, with an example of each`,`Instruct it to "flag anything risky about dependencies"`,`Provide only a temperature setting and let the model decide scope`,`Ask it to vote across three runs without defining the criteria`],
   correct:0, why:`Exact report/skip conditions with examples give the check a concrete, repeatable definition.`,
   traps:{B:`"Anything risky" is undefined and yields inconsistent scope.`,C:`Temperature is not a substitute for defined criteria.`,D:`Voting on undefined criteria cannot create a definition.`}}
],
'few-shot':[
  {q:`Detailed written instructions still yield inconsistent extraction output from your model. Which technique is generally the most effective next step?`,
   options:[`Add 2-4 targeted few-shot examples covering the ambiguous cases, including the reasoning for each choice`,`Rewrite the instructions to be twice as long and more emphatic`,`Lower temperature to 0 and resubmit unchanged`,`Switch to a larger model and keep the same prompt`],
   correct:0, why:`Few-shot examples are the most effective technique when detailed instructions still produce inconsistent output.`,
   traps:{B:`More verbose instructions repeat what already failed; examples teach judgment.`,C:`Temperature reduces variance but does not resolve the ambiguity itself.`,D:`A bigger model still lacks demonstrations of the desired judgment.`}},
  {q:`When choosing which few-shot examples to include for an ambiguous classification task, what makes them most valuable?`,
   options:[`They cover the ambiguous edge cases and include the reasoning for why each was decided that way`,`They are the simplest, most obvious cases so the model learns the basics`,`They are drawn at random to avoid bias`,`They omit reasoning so the model is not constrained`],
   correct:0, why:`2-4 examples covering ambiguous cases with their reasoning teach generalizable judgment.`,
   traps:{B:`Obvious cases were never the source of inconsistency; the edge cases are.`,C:`Random examples may miss the very cases that confuse the model.`,D:`Omitting reasoning removes the signal that teaches generalizable judgment.`}},
  {q:`Your extraction model occasionally hallucinates fields that are not present in the source document. How can few-shot examples help?`,
   options:[`Examples that demonstrate the exact output format and show absent fields handled correctly reduce extraction hallucination`,`Examples raise temperature, which dilutes hallucinated tokens`,`Examples force the API to validate against a schema automatically`,`Examples only help with classification, never extraction`],
   correct:0, why:`Few-shot examples demonstrate the exact output format and reduce extraction hallucination.`,
   traps:{B:`Examples do not change temperature.`,C:`Few-shot prompting does not invoke automatic schema validation.`,D:`Few-shot examples help extraction, including format and hallucination control.`}},
  {q:`A reviewer prompt flags too many acceptable patterns as defects. Which few-shot strategy best cuts these false positives?`,
   options:[`Include paired examples that distinguish an acceptable pattern from a genuine issue, with the reasoning`,`Include only examples of real issues so the model errs toward flagging`,`Include 20+ examples to overwhelm the ambiguity`,`Include examples without labels so the model infers freely`],
   correct:0, why:`Examples distinguishing acceptable patterns from real issues directly cut false positives.`,
   traps:{B:`Only positive examples bias the model toward more false positives.`,C:`2-4 targeted examples suffice; 20+ is unnecessary and may dilute focus.`,D:`Unlabeled examples remove the contrast that teaches the distinction.`}},
  {q:`You want your few-shot examples to lock in a consistent output structure (location, issue, severity, fix). What should each example show?`,
   options:[`A fully formed output with all four fields populated exactly as you want them`,`Only the input, leaving the model to invent the structure`,`The four field names listed but left blank`,`A prose paragraph describing the issue informally`],
   correct:0, why:`Examples that demonstrate the exact output format (location, issue, severity, fix) teach the model the precise structure.`,
   traps:{B:`Inputs alone do not demonstrate the desired output structure.`,C:`Blank fields do not show how the fields should be filled.`,D:`Informal prose contradicts the structured format you want.`}},
  {q:`Before implementing a feature, you want Claude to surface design considerations you might have missed. Which prompting pattern fits best?`,
   options:[`The interview pattern: have Claude ask you clarifying questions first, before writing any code`,`Few-shot examples of finished code`,`A forced tool call with a strict schema`,`A batch job that drafts ten variants overnight`],
   correct:0, why:`The interview pattern (have Claude ask you questions first) surfaces design considerations before implementing.`,
   traps:{B:`Finished-code examples assume the design is already settled.`,C:`A forced schema governs output shape, not design discovery.`,D:`Batching variants does not surface the questions you should answer first.`}},
  {q:`A colleague argues that since the instructions are already detailed, adding examples is redundant. What is the strongest counterpoint?`,
   options:[`Few-shot examples teach generalizable judgment that detailed instructions alone failed to produce consistently`,`Examples are required by the API whenever instructions exceed a length limit`,`Examples replace the need for any instructions at all`,`Examples are only useful for reducing token cost`],
   correct:0, why:`Examples are the most effective technique precisely when detailed instructions still produce inconsistent output.`,
   traps:{B:`There is no API rule tying examples to instruction length.`,C:`Examples complement instructions; they do not replace them.`,D:`Examples can increase token cost; their value is consistency, not savings.`}},
  {q:`How many few-shot examples are typically recommended to teach judgment on ambiguous cases without bloating the prompt?`,
   options:[`2-4 targeted examples focused on the ambiguous cases`,`At least 50 to cover every variation`,`Exactly 1, the canonical case`,`Zero; rely entirely on temperature tuning`],
   correct:0, why:`2-4 targeted examples covering the ambiguous cases is the recommended range.`,
   traps:{B:`50 examples is excessive and bloats the prompt unnecessarily.`,C:`A single example rarely covers the spread of ambiguous cases.`,D:`Temperature tuning does not teach judgment.`}}
],
'structured-output':[
  {q:`You need machine-parseable JSON from Claude and keep hitting malformed-JSON parse failures. What is the most reliable fix?`,
   options:[`Use tool_use with a JSON schema, which eliminates JSON syntax errors`,`Ask for JSON in the prompt and add "return valid JSON only"`,`Post-process the text output with a regex JSON repair pass`,`Lower temperature to 0 and hope for clean braces`],
   correct:0, why:`tool_use with a JSON schema is the most reliable way to get schema-valid output and eliminates JSON syntax errors.`,
   traps:{B:`A prompt instruction does not guarantee syntactically valid JSON.`,C:`Regex repair is brittle and reactive, not a reliable guarantee.`,D:`Temperature reduces variance but does not guarantee valid structure.`}},
  {q:`You must guarantee the model calls a tool (rather than replying in prose) on every request. Which setting achieves this?`,
   options:[`Set tool_choice to "any" so a tool call is guaranteed`,`Set tool_choice to "auto" and add a strong instruction`,`Set temperature to 0`,`Provide only one tool and rely on the model to notice`],
   correct:0, why:`tool_choice "any" guarantees that the model makes a tool call.`,
   traps:{B:`"auto" lets the model choose to answer in prose instead.`,C:`Temperature does not force a tool call.`,D:`Offering one tool does not force the model to invoke it.`}},
  {q:`You need to guarantee that a specific tool (not just any tool) is invoked, in a defined order. Which configuration does this?`,
   options:[`Force the tool with tool_choice {"type":"tool","name":"…"}`,`Use tool_choice "any" and hope the right one is picked`,`List the desired tool first and rely on positional bias`,`Set tool_choice "auto" with a detailed description`],
   correct:0, why:`A forced tool_choice of type tool with a name guarantees that specific tool is called.`,
   traps:{B:`"any" guarantees a tool call but not which tool.`,C:`Ordering alone does not guarantee the specific tool is chosen.`,D:`"auto" may not call the tool at all.`}},
  {q:`Your strict schema eliminated all JSON syntax errors, yet invoices still come back where the line items do not sum to the stated total. Why?`,
   options:[`Strict schemas kill syntax errors but not semantic errors like a sum mismatch`,`The schema must have been invalid; strict schemas catch arithmetic too`,`tool_choice was set to "auto" instead of "any"`,`Temperature was above 0, corrupting the numbers`],
   correct:0, why:`A strict schema guarantees valid structure but cannot catch semantic errors such as line items not summing to the total.`,
   traps:{B:`Schemas validate shape and types, not arithmetic relationships.`,C:`tool_choice governs whether a tool is called, not arithmetic validity.`,D:`Temperature is not why the math fails to reconcile.`}},
  {q:`Some source documents lack a "tax_id" field, and your required-field schema causes the model to fabricate one. What is the right schema change?`,
   options:[`Make the field nullable/optional so the model returns null instead of fabricating a value`,`Keep it required but add "do not guess" to the prompt`,`Force a tool call so the field is always populated`,`Raise temperature so the model varies its guesses`],
   correct:0, why:`Making fields nullable/optional lets the model return null when the source lacks them, instead of fabricating to satisfy a required field.`,
   traps:{B:`A required field still pressures the model to invent a value.`,C:`Forcing a tool call does not make a missing value present.`,D:`Higher temperature produces more varied fabrications, not fewer.`}},
  {q:`Your classification schema uses a fixed enum, but real inputs occasionally fall outside the defined categories and the model jams them into a wrong bucket. What schema design handles this?`,
   options:[`Add an extensible "other" value with a detail string (and "unclear" for ambiguous cases)`,`Remove the enum and accept free-text labels`,`Add more required fields to compensate`,`Force the closest enum match via the prompt`],
   correct:0, why:`Extensible enums ("other" plus a detail string, "unclear" for ambiguous cases) handle out-of-enum inputs cleanly.`,
   traps:{B:`Free text discards the benefit of a controlled vocabulary entirely.`,C:`Extra required fields do not address the out-of-enum problem.`,D:`Forcing the closest match is exactly the wrong-bucket behavior you want to avoid.`}},
  {q:`Which statement best captures the boundary of what a strict tool_use JSON schema can guarantee?`,
   options:[`It guarantees syntactically valid, schema-conformant output but not semantic correctness of the values`,`It guarantees both valid syntax and correct arithmetic`,`It guarantees the model will call the correct tool every time without tool_choice`,`It guarantees absent source fields are filled with accurate values`],
   correct:0, why:`Strict schemas eliminate syntax/structure errors but cannot ensure the values are semantically correct.`,
   traps:{B:`Schemas do not validate arithmetic.`,C:`Tool selection requires tool_choice; a schema alone does not force it.`,D:`Schemas cannot supply values absent from the source.`}},
  {q:`A field in your extraction frequently has no answer in the source, yet you want the model to be honest about that. Which combination is best?`,
   options:[`Make the field nullable so the model can return null when the data is genuinely absent`,`Mark it required and lower temperature to discourage guessing`,`Use tool_choice "any" so the field is always emitted with a value`,`Add a second required copy of the field for redundancy`],
   correct:0, why:`A nullable field lets the model return null for genuinely absent data rather than fabricating a value.`,
   traps:{B:`A required field still pushes toward fabrication regardless of temperature.`,C:`Forcing a tool call still demands a value for a required field.`,D:`Duplicating a required field doubles the fabrication, not the honesty.`}}
],
'validation-retry':[
  {q:`Your first extraction attempt fails a structural validation check. What retry strategy best lets the model self-correct?`,
   options:[`Send a follow-up with the original document, the failed extraction, and the specific validation error`,`Resubmit the same prompt unchanged and hope for variance`,`Lower temperature and retry without mentioning the error`,`Switch to the Batches API for the retry`],
   correct:0, why:`Retry-with-error-feedback (original document, failed extraction, and the specific error) lets the model self-correct.`,
   traps:{B:`Blind resubmission gives the model nothing to correct.`,C:`Without the specific error, the model cannot target the fix.`,D:`Batching changes throughput, not the self-correction mechanism.`}},
  {q:`A required value is simply not present anywhere in the source document. Why will retry-with-error-feedback fail to fix the extraction?`,
   options:[`Retry feedback corrects format and structural errors, but it is useless when the required info is absent from the source`,`Retry feedback only works at temperature 0`,`Retry feedback requires the Batches API to function`,`Retry feedback can only run once per document`],
   correct:0, why:`Retry-with-error-feedback works for format/structural errors but is useless when the required info is absent from the source.`,
   traps:{B:`Temperature is unrelated to whether the data exists in the source.`,C:`Retry feedback does not depend on the Batches API.`,D:`There is no inherent one-retry limit; the issue is missing data.`}},
  {q:`You want to automatically flag invoices where the extracted line items do not match the stated total. Which design catches this semantic error?`,
   options:[`Extract a calculated_total alongside the stated_total and flag mismatches`,`Tighten the JSON schema to forbid mismatches`,`Set tool_choice to "any"`,`Lower temperature so the math is more accurate`],
   correct:0, why:`Extracting calculated_total alongside stated_total adds a semantic self-check that flags mismatches a schema cannot.`,
   traps:{B:`A schema validates shape, not whether numbers reconcile.`,C:`tool_choice forces a tool call, not arithmetic validation.`,D:`Temperature does not enforce that totals reconcile.`}},
  {q:`You want a structured signal whenever the source contains contradictory information the model had to reconcile. What is a clean way to surface this?`,
   options:[`Add a conflict_detected boolean field to the output schema`,`Ask the model to mention conflicts in free-text prose`,`Increase temperature so conflicts become visible`,`Run the extraction twice and diff the outputs`],
   correct:0, why:`A conflict_detected boolean gives a structured, queryable signal of contradictions in the source.`,
   traps:{B:`Free-text mentions are hard to parse and easy to miss programmatically.`,C:`Temperature does not expose source conflicts.`,D:`Diffing two runs detects model variance, not source contradictions specifically.`}},
  {q:`Your reviewer produces false positives and you want to analyze them systematically over time. Which output addition helps most?`,
   options:[`Track a detected_pattern field so you can later analyze which patterns drive false positives`,`Remove all metadata to keep outputs small`,`Raise temperature to diversify the findings`,`Disable structured output and read the prose`],
   correct:0, why:`A detected_pattern field lets you analyze which patterns drive false positives over time.`,
   traps:{B:`Stripping metadata removes the very signal needed for analysis.`,C:`More randomness obscures rather than clarifies the patterns.`,D:`Unstructured prose is far harder to aggregate and analyze.`}},
  {q:`Self-review by the same model instance is missing errors in its own extractions. What is a stronger validation approach?`,
   options:[`Use an independent reviewer instance to check the output, since it beats self-review`,`Ask the same instance to review its work three more times`,`Raise the temperature on the self-review pass`,`Trust the strict schema to catch the errors instead`],
   correct:0, why:`An independent reviewer instance beats self-review for catching errors.`,
   traps:{B:`Repeated self-review shares the same blind spots.`,C:`Temperature does not give the model independence from its own output.`,D:`A schema catches structure, not the semantic errors self-review misses.`}},
  {q:`When retry-with-error-feedback IS appropriate, which inputs must the follow-up message include for the model to self-correct effectively?`,
   options:[`The original document, the failed extraction, and the specific validation error`,`Only the specific validation error, to keep the prompt short`,`Only the original document, so the model starts fresh`,`A larger model name and nothing else`],
   correct:0, why:`The follow-up must include the original document, the failed extraction, and the specific validation error.`,
   traps:{B:`The error alone, without the document and prior attempt, gives no context to fix.`,C:`Starting fresh discards the failed attempt and the targeted error.`,D:`Swapping models is not the retry-with-feedback mechanism.`}}
],
'batch':[
  {q:`A pre-merge CI check blocks the developer until results return. Is the Message Batches API a good fit, and why?`,
   options:[`No — batches have no latency SLA and up to a 24-hour window, so they are wrong for blocking work where a developer waits`,`Yes — the ~50% discount outweighs any latency concern`,`Yes — batches guarantee sub-second turnaround for small jobs`,`No — batches cannot return JSON output at all`],
   correct:0, why:`Batches have no latency SLA and a window up to 24 hours, so they must never be used for blocking pre-merge checks.`,
   traps:{B:`Cost savings do not justify blocking a waiting developer with unbounded latency.`,C:`Batches offer no sub-second guarantee; there is no latency SLA.`,D:`Batches can return JSON; the issue is latency, not output format.`}},
  {q:`You run a weekly, latency-tolerant audit of an entire repository overnight. Which API choice optimizes cost without hurting the workflow?`,
   options:[`The Message Batches API, which is ~50% cheaper and fits non-blocking, latency-tolerant work`,`The standard synchronous API for predictable speed`,`A forced tool call on each file synchronously`,`Streaming responses to reduce cost`],
   correct:0, why:`The Batches API is ~50% cheaper and ideal for non-blocking, latency-tolerant work like overnight or weekly audits.`,
   traps:{B:`Synchronous calls forgo the ~50% batch discount with no benefit for overnight work.`,C:`Forcing tool calls does not address cost or batching.`,D:`Streaming affects delivery, not the batch cost discount.`}},
  {q:`After submitting a batch of 5,000 requests, how do you match each response back to the request that produced it?`,
   options:[`Use the custom_id you assigned to each request to correlate request/response pairs`,`Rely on the response array preserving submission order`,`Match by request timestamp`,`Hash the response body and look it up`],
   correct:0, why:`custom_id correlates request/response pairs in the Batches API.`,
   traps:{B:`Batch responses are not guaranteed to preserve submission order.`,C:`Timestamps are not a reliable correlation key.`,D:`Hashing the response does not map it back to a specific input.`}},
  {q:`Out of 5,000 batched requests, 80 failed. What is the recommended way to recover?`,
   options:[`Resubmit only the failed custom_ids rather than the entire batch`,`Resubmit the whole batch to be safe`,`Switch the failed ones to the synchronous API permanently`,`Discard the failures since batches are best-effort`],
   correct:0, why:`Resubmit only the failed custom_ids rather than reprocessing the entire batch.`,
   traps:{B:`Reprocessing all 5,000 wastes cost when only 80 failed.`,C:`Permanently switching APIs is unnecessary; just resubmit the failures.`,D:`Discarding failures loses data you can simply resubmit.`}},
  {q:`Your batch workflow needs a multi-turn tool-calling conversation within a single request. What constraint of the Batches API applies?`,
   options:[`The Batches API does not support multi-turn tool calling within a single request`,`The Batches API supports unlimited multi-turn tool calling`,`Multi-turn tool calling only works if tool_choice is "any"`,`Multi-turn tool calling requires a 24-hour window flag`],
   correct:0, why:`The Batches API does not support multi-turn tool calling in a single request.`,
   traps:{B:`That is the opposite of the actual constraint.`,C:`tool_choice does not enable multi-turn within a batch request.`,D:`The 24-hour window is the processing limit, not a multi-turn enabler.`}},
  {q:`Roughly what cost advantage does the Message Batches API offer over synchronous requests?`,
   options:[`About 50% cheaper`,`About 10% cheaper`,`Identical cost, only faster`,`About 90% cheaper`],
   correct:0, why:`The Message Batches API is approximately 50% cheaper than synchronous requests.`,
   traps:{B:`The discount is around 50%, not 10%.`,C:`Batches are cheaper and not faster; there is no latency SLA.`,D:`The discount is around 50%, not 90%.`}},
  {q:`Before launching a 20,000-request batch, how can you maximize the first-pass success rate?`,
   options:[`Refine your prompt on a small sample first, then submit the full batch`,`Submit all 20,000 immediately to get results sooner`,`Set the window to its minimum so failures surface fast`,`Use tool_choice "auto" to let the model self-correct`],
   correct:0, why:`Refining the prompt on a sample first lifts first-pass success before committing the full batch.`,
   traps:{B:`Launching unvalidated risks 20,000 low-quality results.`,C:`A shorter window does not improve per-request quality.`,D:`"auto" does not address prompt quality across the batch.`}},
  {q:`A team wants overnight report generation but worries the 24-hour window means results could take a full day. What is accurate about batch timing?`,
   options:[`Up to 24 hours is the maximum window with no latency SLA; many batches finish well before, which suits overnight work`,`Batches always take exactly 24 hours to complete`,`Batches guarantee completion within one hour`,`The window can be reduced to force a 5-minute SLA`],
   correct:0, why:`The 24-hour window is a maximum with no latency SLA; overnight, latency-tolerant work fits this model well.`,
   traps:{B:`24 hours is the maximum, not a fixed duration.`,C:`There is no one-hour completion guarantee.`,D:`There is no configurable SLA on batches.`}}
],
/* ---- Domain 5 ---- */
'context-preservation':[
  {q:`Over a 40-turn support chat, progressive summarization has blurred "$347.50 refund on order #88231" into "a refund on the customer's order." What design keeps the exact values intact?`,
   options:[`Maintain a persistent "case facts" block (amounts, order IDs, statuses) outside the summarized history and re-include it every prompt`,`Summarize more aggressively so the important facts rise to the top`,`Lower the temperature so the model recalls numbers more precisely`,`Trust the model to reconstruct the numbers from the conversation`],
   correct:0, why:`A structured facts block kept outside summarized prose preserves precise values no matter how the history is compressed.`,
   traps:{B:`More summarization blurs the very numbers you need to keep.`,C:`Temperature doesn't restore information that summarization already dropped.`,D:`Reconstructing exact values from blurred history is exactly what fails.`}},
  {q:`A 30,000-token transcript has critical account details buried in the middle, and the agent keeps overlooking them. Which fix best addresses the "lost in the middle" effect?`,
   options:[`Move the key facts and summaries to the top (and end), with explicit section headers`,`Add more detail to the middle so it stands out`,`Increase max_tokens so the whole transcript fits`,`Re-order the transcript randomly each turn`],
   correct:0, why:`Models read the start and end most reliably; placing key facts there (with headers) counters the middle being skimmed.`,
   traps:{B:`Adding bulk to the middle keeps the facts in the least-reliable region.`,C:`Fitting more tokens doesn't change where the model attends within them.`,D:`Random ordering doesn't guarantee facts land in a reliable position.`}},
  {q:`Every order lookup returns 40 fields but only 5 matter for the conversation, and context is filling up. Best practice?`,
   options:[`Trim each tool output to the relevant fields before it accumulates in history`,`Keep all 40 fields for completeness in case they're needed later`,`Summarize the 40 fields into a vague prose sentence`,`Stop calling the order-lookup tool entirely`],
   correct:0, why:`Trimming verbose tool outputs to the relevant fields keeps context lean and on-point.`,
   traps:{B:`Hoarding 35 irrelevant fields per call is exactly what bloats and degrades context.`,C:`Vague prose loses the precise field values you may still need.`,D:`Dropping the tool loses the 5 fields that do matter.`}},
  {q:`In a long codebase-exploration session, the model starts citing "typical patterns" instead of the specific classes it found earlier. Best mitigation?`,
   options:[`Keep a scratchpad file of key findings to reference later, and use <code>/compact</code> to reclaim room as context fills`,`Push on in the same session; the model will recover on its own`,`Start a brand-new session with no context from the exploration`,`Ask the model to "try harder to remember" the earlier classes`],
   correct:0, why:`Scratchpad files persist findings across context boundaries and <code>/compact</code> reclaims room, countering degradation.`,
   traps:{B:`Degradation worsens as context grows; it doesn't self-heal.`,C:`A blank session discards everything already learned.`,D:`Exhortation can't restore information that has fallen out of context.`}},
  {q:`Your multi-agent codebase exploration crashes after two hours and loses all progress. How should it have been designed for recovery?`,
   options:[`Have each agent export structured state to a known location; the coordinator loads a manifest on resume and reinjects it`,`Restart the entire exploration from scratch each time`,`Hold all state in one ever-growing context window`,`Rely on the model to remember where it left off`],
   correct:0, why:`Structured state exports plus a coordinator manifest let work resume after a crash instead of restarting.`,
   traps:{B:`Restarting discards all prior work — the failure being avoided.`,C:`A single huge window still vanishes on a crash and exhausts capacity.`,D:`Models have no durable memory across a crash.`}},
  {q:`An extraction pipeline reports 97% overall accuracy, so the team wants to drop human review. What must you verify first?`,
   options:[`Break accuracy down by document type and field — aggregate numbers can mask poor performance on a specific segment`,`Nothing — 97% aggregate is clearly good enough to ship`,`Re-run until the aggregate climbs to 99%`,`Remove human review now and watch for complaints later`],
   correct:0, why:`Aggregate metrics can hide a failing segment, so validate by document type and field before reducing oversight.`,
   traps:{B:`A high aggregate can still conceal a badly failing document type or field.`,C:`A higher aggregate still hides per-segment gaps.`,D:`Pulling review before checking segments risks shipping bad extractions.`}}
],
'escalation':[
  {q:`A customer writes "Just connect me to a human, please." Mid-troubleshooting, what should the agent do?`,
   options:[`Honor the explicit request and escalate to a human immediately, without first attempting more investigation`,`Run two more diagnostic steps, then escalate only if they fail`,`Offer a discount to keep the conversation automated`,`Ask three clarifying questions before deciding`],
   correct:0, why:`An explicit human request is honored immediately — it's one of the sound escalation triggers, with no investigation-first requirement.`,
   traps:{B:`Continuing to investigate ignores the explicit, valid request to escalate.`,C:`A discount neither honors the request nor resolves the issue.`,D:`More questions delay an escalation the customer already asked for.`}},
  {q:`The agent encounters a refund scenario the policy simply doesn't cover — a genuine exception. What's the right move?`,
   options:[`Escalate: a policy gap/exception is a valid trigger when the rulebook doesn't address the case`,`Pick the closest policy and apply it as if it fit`,`Deny the request since no policy explicitly allows it`,`Approve it to keep the customer happy`],
   correct:0, why:`Policy gaps and exceptions are a legitimate escalation trigger; the agent shouldn't improvise a binding decision.`,
   traps:{B:`Forcing an ill-fitting policy onto an exception risks a wrong, binding outcome.`,C:`A blanket denial on a gap can be just as wrong as a blanket approval.`,D:`Approving to please the customer ignores that the case needs human judgment.`}},
  {q:`Your team proposes routing any ticket where the agent self-reports confidence below 0.7 to a human. Why is this a weak calibration signal?`,
   options:[`Self-reported confidence is poorly calibrated — the agent is often confidently wrong on the hardest cases`,`Confidence scores are accurate but 0.7 is simply the wrong threshold`,`Confidence routing works only for billing tickets`,`Confidence below 0.7 never actually occurs in practice`],
   correct:0, why:`Self-reported confidence is an unreliable proxy for case complexity; the model is frequently confident on cases it gets wrong.`,
   traps:{B:`No threshold rescues a fundamentally miscalibrated signal.`,C:`The calibration problem isn't limited to one ticket type.`,D:`Low self-reported confidence does occur; the issue is that it's unreliable.`}},
  {q:`An agent escalates whenever a customer's message reads as angry. Why is sentiment a poor escalation trigger?`,
   options:[`Sentiment doesn't correlate with case complexity — a frustrated customer may have a simple, in-scope issue`,`Sentiment analysis is too slow to run per message`,`Angry customers always actually want a refund, not escalation`,`Sentiment scoring isn't available to agents`],
   correct:0, why:`Sentiment is an unreliable proxy for whether a case actually needs a human; complexity, not tone, should drive escalation.`,
   traps:{B:`Latency isn't the objection; the signal itself is the wrong proxy.`,C:`Tone doesn't reliably indicate what the customer wants.`,D:`Availability isn't the issue; correlation with complexity is.`}},
  {q:`Your support bot sits at 55% first-contact resolution (target 80%): it escalates routine photo-evidence replacements while trying to handle policy exceptions itself. Most proportionate first fix?`,
   options:[`Add explicit escalation criteria to the prompt with few-shot examples showing when to escalate vs resolve`,`Have the agent emit a 1-10 confidence score and route low scores to humans`,`Train a separate ML classifier on historical tickets to predict escalation`,`Escalate on negative sentiment to catch the hard cases`],
   correct:0, why:`The decision boundary is unclear; explicit criteria plus few-shot examples is the proportionate fix before adding infrastructure.`,
   traps:{B:`Self-reported confidence is poorly calibrated — already confidently wrong on hard cases.`,C:`A trained classifier is over-engineered before prompt optimization is even tried.`,D:`Sentiment doesn't track complexity, so it won't fix the calibration.`}},
  {q:`A customer lookup by name returns three different people. What should the agent do before taking any account action?`,
   options:[`Ask for an additional identifier (email, order number) to disambiguate rather than guessing`,`Act on the first match returned`,`Choose the account with the most orders as most likely`,`Proceed on all three accounts to be safe`],
   correct:0, why:`Multiple matches require clarification; requesting another identifier avoids acting on the wrong account.`,
   traps:{B:`First-match is an arbitrary guess that can hit the wrong person.`,C:`"Most orders" is still a guess, not a verified match.`,D:`Acting on all three risks changing unrelated accounts.`}},
  {q:`A customer is frustrated but their request (a standard return within policy) is clearly in scope. Best handling?`,
   options:[`Acknowledge the frustration and resolve the in-scope request; escalate only if they reiterate a demand for a human`,`Escalate immediately because they're upset`,`Ignore the emotion and process the return silently`,`Offer a coupon and close the ticket without processing the return`],
   correct:0, why:`When a request is in scope, acknowledge the emotion and resolve it; escalate only on an explicit, repeated request.`,
   traps:{B:`Frustration alone isn't an escalation trigger when the case is in scope.`,C:`Ignoring the emotion handles the task but not the customer.`,D:`A coupon dodges the actual request the customer made.`}},
  {q:`The agent has tried every available tool and still cannot make progress on a stuck case. What is the appropriate action?`,
   options:[`Escalate — genuine inability to make progress is a valid escalation trigger`,`Loop the same tools again hoping for a different result`,`Close the ticket as resolved to protect the resolution rate`,`Fabricate a plausible answer so the customer has something`],
   correct:0, why:`Inability to make progress is one of the three sound escalation triggers; hand off rather than spin or fake a result.`,
   traps:{B:`Re-running the same tools that already failed won't make progress.`,C:`Marking an unresolved case resolved hides the failure and harms the customer.`,D:`Fabricating an answer is the worst outcome of all.`}}
],
'error-propagation':[
  {q:`A web-search subagent times out on a hard query. Which return best enables the coordinator to recover well?`,
   options:[`Structured error context: failure type, the attempted query, any partial results, and suggested alternatives`,`A generic "search unavailable" string after exhausting internal retries`,`An empty result marked as a success`,`An exception thrown to a top-level handler that ends the whole workflow`],
   correct:0, why:`Structured context lets the coordinator retry differently, try an alternative, or proceed with partial results and annotate the gap.`,
   traps:{B:`A generic status hides the context needed for an informed recovery decision.`,C:`Masking failure as success prevents recovery and risks incomplete output.`,D:`Terminating everything is unnecessary when targeted recovery could succeed.`}},
  {q:`A search subagent runs successfully but matches nothing. How should it report this so the coordinator reacts correctly?`,
   options:[`As a valid empty result (successful query, no matches) — distinct from an access failure that needs a retry decision`,`As an error so the coordinator retries the query`,`As a failure that terminates the research workflow`,`As a transient timeout so it's retried with backoff`],
   correct:0, why:`An empty result is a successful outcome and must be distinguished from an access failure that may warrant a retry.`,
   traps:{B:`Retrying a successful empty query just returns the same empty set.`,C:`A legitimate empty result isn't grounds to kill the workflow.`,D:`Nothing timed out; it's a success, not a transient fault.`}},
  {q:`A subagent catches its own failure and quietly returns an empty payload marked successful. Why is this a dangerous anti-pattern?`,
   options:[`Silent suppression hides the failure from the coordinator, so it can't recover and may emit incomplete output as if complete`,`It's fine because empty output is always safe`,`It improves throughput by avoiding error handling`,`It only matters when more than half the subagents fail`],
   correct:0, why:`Suppressing errors as empty success removes the signal the coordinator needs and risks shipping incomplete results unknowingly.`,
   traps:{B:`Empty-as-success is not safe; it conceals a real gap.`,C:`Skipping error handling trades correctness for nothing real.`,D:`Even one silently suppressed failure can corrupt the final output.`}},
  {q:`One of six research subagents fails. The pipeline throws to a top-level handler that aborts the entire run. What's wrong with this design?`,
   options:[`Terminating the whole workflow on a single failure is an anti-pattern — recovery or partial results from the other five could have succeeded`,`Nothing; any failure should stop everything for safety`,`The handler should silently swallow the error instead`,`The five successful subagents should also be marked failed`],
   correct:0, why:`Killing everything on one failure throws away recoverable progress; the coordinator should recover locally or proceed with partial results.`,
   traps:{B:`Halting on any single failure wastes the work that did succeed.`,C:`Silent swallowing is the opposite anti-pattern, also wrong.`,D:`Marking good results failed destroys valid work.`}},
  {q:`A subagent hits a transient network blip it could retry past on its own. Where should recovery happen?`,
   options:[`Locally inside the subagent for transient issues; propagate only what it genuinely can't resolve, with partial results`,`Always propagate every error straight to the coordinator`,`Escalate every transient blip to a human`,`Abort the subagent and return nothing`],
   correct:0, why:`Subagents should recover locally for transient issues and propagate only unresolved failures, carrying partial results forward.`,
   traps:{B:`Propagating trivially recoverable blips burdens the coordinator needlessly.`,C:`Human escalation for a transient blip is wildly disproportionate.`,D:`Returning nothing discards partial progress and context.`}},
  {q:`A subagent returns only "operation failed" after several internal retries. Why does this still hinder the coordinator?`,
   options:[`It strips the failure type, attempted query, and partial results the coordinator needs to choose a recovery path`,`It's ideal because it keeps the response short`,`It guarantees the coordinator will retry correctly`,`It tells the coordinator the empty result is valid`],
   correct:0, why:`A generic status hides the structured context — type, attempt, partials — required for an informed recovery decision.`,
   traps:{B:`Brevity isn't the goal; the missing context is the problem.`,C:`Without context the coordinator can't know how to retry.`,D:`A generic failure says nothing about whether an empty result is valid.`}},
  {q:`How should a failing subagent represent work it managed to complete before the failure?`,
   options:[`Include the partial results in its structured error so the coordinator can use or annotate them`,`Discard partial results since the call ultimately failed`,`Return the partials as a clean success with no error flag`,`Hold the partials internally and report only the error`],
   correct:0, why:`Returning partial results alongside the structured error lets the coordinator salvage progress and annotate any gap.`,
   traps:{B:`Discarding partials throws away usable work.`,C:`Marking a failed call a clean success masks the gap in the partials.`,D:`Withholding the partials denies the coordinator usable data.`}},
  {q:`A coordinator must decide whether to retry, reroute, or proceed after a subagent issue. What does it most need from the subagent's return?`,
   options:[`A structured report distinguishing the failure type and including the attempt, partial results, and alternatives`,`A single boolean success/failure flag`,`The subagent's full internal reasoning transcript`,`A natural-language apology for the failure`],
   correct:0, why:`Structured, categorized context is exactly what lets the coordinator pick retry, reroute, or proceed-with-partials.`,
   traps:{B:`A bare boolean can't distinguish a transient failure from a valid empty result.`,C:`A raw reasoning dump bloats context without the decision-relevant fields.`,D:`An apology carries no actionable recovery information.`}}
],
'provenance':[
  {q:`Synthesizing a market report, two credible sources give different market-size figures. Before synthesis, what should the analysis subagent do?`,
   options:[`Keep both values, each explicitly annotated with its source, and let the coordinator decide how to reconcile`,`Silently choose the value from the more recent source`,`Average the two figures into one number`,`Discard both as unreliable since they disagree`],
   correct:0, why:`Preserving both conflicting values with attribution lets downstream synthesis reconcile transparently instead of hiding the disagreement.`,
   traps:{B:`Silent selection destroys the conflict signal the coordinator needs.`,C:`Averaging fabricates a number neither source actually reported.`,D:`Discarding loses real, credible data.`}},
  {q:`A final report can no longer say which source backs each claim. What should subagents emit to preserve attribution through synthesis?`,
   options:[`Structured claim-source mappings (URL, document name, excerpt, date) that downstream agents carry through`,`Plain prose summaries with the sources stripped to save tokens`,`Only the final conclusions, without any source references`,`The raw web pages pasted in full`],
   correct:0, why:`Structured claim-source mappings let each claim stay traceable to its origin as it flows through synthesis.`,
   traps:{B:`Stripping sources from prose is exactly how attribution gets lost.`,C:`Conclusions without references can't be traced back at all.`,D:`Raw page dumps bloat context and still aren't structured mappings.`}},
  {q:`Two sources report different unemployment rates — but one is from 2021 and the other from 2024. How should synthesis treat this?`,
   options:[`Include the collection dates so the difference is read as a temporal change, not a contradiction`,`Flag it as a direct conflict and pick one value`,`Average the two rates`,`Drop the older figure as outdated`],
   correct:0, why:`Publication/collection dates let synthesis interpret time-based differences correctly rather than mistaking them for conflicts.`,
   traps:{B:`Treating a time difference as a contradiction misrepresents the data.`,C:`Averaging across different years produces a meaningless number.`,D:`The older figure may be valid for its period; dropping it loses context.`}},
  {q:`A research report mixes well-established findings with a few contested claims. How should it be structured for the reader?`,
   options:[`Separate well-established findings from contested ones so reliability is explicit`,`Present every claim with identical confidence for a clean read`,`Lead with the contested claims because they're most interesting`,`Omit the contested claims to avoid confusing the reader`],
   correct:0, why:`Separating established from contested findings makes each section's reliability explicit to the reader.`,
   traps:{B:`Uniform confidence misleads by hiding which claims are shaky.`,C:`Foregrounding contested claims overstates their certainty.`,D:`Omitting contested claims hides real, relevant disagreement.`}},
  {q:`Some sources were unavailable, so parts of the report rest on thin support. How should synthesis represent that?`,
   options:[`Add coverage notes marking which findings are well-supported and which have gaps from unavailable sources`,`Present the thin sections with the same confidence as the rest`,`Silently omit the thinly-supported sections`,`Fill the gaps with plausible-sounding content`],
   correct:0, why:`Explicit coverage annotations make each section's reliability clear rather than overstating or hiding the gaps.`,
   traps:{B:`Equal confidence misleads about the thin sections.`,C:`Silent omission hides that coverage was incomplete.`,D:`Fabricating to fill gaps is the worst possible outcome.`}},
  {q:`A summarization step compresses ten sources into three paragraphs and loses every citation. What design preserves attribution?`,
   options:[`Require the summary to retain claim-to-source mappings (source name, excerpt, date) alongside the compressed text`,`Compress harder so only the highest-confidence claims survive`,`Keep the prose and append a single generic "sources: various" note`,`Accept the loss since summaries are meant to be concise`],
   correct:0, why:`Carrying claim-to-source mappings through the compression keeps every retained claim traceable.`,
   traps:{B:`Harder compression drops even more attribution, not less.`,C:`"Sources: various" can't tie any specific claim to its origin.`,D:`Conciseness doesn't require discarding traceability.`}},
  {q:`The coordinator wants to reconcile a conflict between two credible statistics itself. What must the subagents give it to do that fairly?`,
   options:[`Both values with full attribution (source, date, excerpt), not a single pre-chosen number`,`Only the value the subagent judged most trustworthy`,`The averaged figure to save the coordinator a step`,`A note that the sources conflicted, without the actual numbers`],
   correct:0, why:`The coordinator can only reconcile fairly if it receives both attributed values, not a pre-filtered or fabricated one.`,
   traps:{B:`Pre-choosing one value strips the coordinator's ability to reconcile.`,C:`A pre-averaged figure hides both real numbers behind a fabricated one.`,D:`"They conflicted" without the values gives nothing to reconcile.`}},
  {q:`Why is silently picking the higher of two conflicting statistics a poor synthesis choice?`,
   options:[`It destroys the conflict signal and hides a real disagreement between credible sources from the reader`,`It's fine as long as the higher number is from a newer source`,`Higher numbers are statistically more likely to be correct`,`It saves tokens, which outweighs the lost attribution`],
   correct:0, why:`Arbitrarily selecting one value hides a genuine disagreement that the reader should see, with attribution, to judge.`,
   traps:{B:`Recency doesn't justify silently dropping the other credible value.`,C:`There's no rule that larger figures are more accurate.`,D:`Token savings don't justify concealing a real conflict.`}}
],
};
